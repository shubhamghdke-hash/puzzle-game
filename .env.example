# Google AI Studio key — AIzaSy... (standard) or AQ.... (auth key, uses x-goog-api-key header)
GEMINI_API_KEY=

# Optional
# PORT=8765
# HOST=127.0.0.1
# HOST=0.0.0.0   # use this to test on your phone over Wi‑Fi
# UPLOADS_DIR=uploads   # where saved photos go locally (default: uploads/)
# SAVE_UPLOADS=true     # set false to disable saving uploads
# BLOB_READ_WRITE_TOKEN=  # Vercel Blob token for saving uploads in production
